<Project>
  <ItemGroup>
    <Compile Update="@(Compile)">
      <Visible Condition="'%(NuGetItemType)' == 'Compile' and '%(NuGetPackageId)' == 'Simulation.Core'">false</Visible>
    </Compile>
  </ItemGroup>
</Project>